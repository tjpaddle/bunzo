[package]
name = "bunzo-shell"
version = "0.0.1"
edition = "2021"
description = "bunzo chat shell — minimal TUI stub (M2)"
license = "MIT"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
