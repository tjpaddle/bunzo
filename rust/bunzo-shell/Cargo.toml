[package]
name = "bunzo-shell"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "bunzo chat shell — serial / TUI front-end"

[dependencies]
bunzo-proto = { workspace = true }
crossterm = "0.28"
libc = "0.2"
ratatui = "0.29"
